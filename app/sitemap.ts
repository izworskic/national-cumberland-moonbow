import type { MetadataRoute } from "next";
export default function sitemap(): MetadataRoute.Sitemap { const base = process.env.NEXT_PUBLIC_SITE_URL ?? "https://moonbow-window.vercel.app"; return ["/cumberland-falls-moonbow", "/cumberland-falls-moonbow/2026", "/cumberland-falls-moonbow/september-2026", "/cumberland-falls-moonbow/best-time", "/cumberland-falls-moonbow/photography", "/cumberland-falls-moonbow/methodology"].map((path, index) => ({ url: `${base}${path}`, lastModified: new Date("2026-09-08"), changeFrequency: index ? "monthly" : "hourly", priority: index ? 0.7 : 1 })); }
